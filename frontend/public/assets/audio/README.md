# 音频素材预留目录

把音频文件放在这里即可接入页面：

- `background-music.wav`：页面打开后自动尝试循环播放的背景音乐。
- `click-sound.wav`：按钮、导航等交互点击音效。

说明：

- 浏览器可能会阻止带声音的自动播放。页面会先在打开时尝试播放，如果被阻止，会在用户第一次点击页面后继续播放。
- 请只放入你拥有使用权限或授权允许使用的音频文件。
- 当前目录中的两个 `.wav` 文件是静音占位文件。后续可以直接用同名音频文件覆盖。
